" mkdir vimfiles
" mkdir .vimundos
" google suchen nach vim-plug
" junegunn/vim-plug: :hibiscus: Minimalist Vim Plugin Manager
" dort nach Download plug.vim and put it in the "autoload" directory. suchen und die plug.vim downloaden
" im verzeichnis vimfiles/autoload eine datei mit dem namen plug.vim anlegen und den inhalt der obrigen Datei reinkopieren.
" Beginn Plugin Vervollständigung

set nocompatible
filetype off

set number
" Es werden die relativen Zeilennummer am rand angezeig aktuellen Positionen am Rand 
set relativenumber
" setzt suche groß und klein außer kraft
set ignorecase
" set wenn doch ein großbuchtabe eingegeben wir case insenstiv wieder aus
set smartcase 

" Hiermit wird die Rechtschreibung mit geladen
:map <F12> :w!<CR>:!aspell --lang=german check %<CR>:e! %<CR>
" Plug ins installieren
call plug#begin()

" Plugins hier einfügen
Plug 'mbbill/undotree'
Plug 'davidhalter/jedi-vim'

" ipython Plugin
" Plug 'goerz/jupytext.vim'
" Plug 'jpalardy/vim-slime'
" Plug 'hanschen/vim-ipython-cell'
" start durch :Ipython

 Plug 'scrooloose/nerdtree'

call plug#end()
" Ende Plugin
" 


" ========================================
" Automatisches laden der .vimrc
" ========================================
autocmd! bufwritepost .vimrc source %

" sollte eingestellt sein damit wir nicht auf den Funktionsumfang von vi herabgestufft werden.
set nocompatible

" No backupfile damit wir nicht im Dateiverzeichnis swb files bekommen
set nobackup
set incsearch 
" Besseres copy & paste
set autoindent
" Damit wird sicher gestelle das man den Buffer wechseln kann ohne ihn spreicher zu müssen.
set hidden
set encoding=utf-8
" Wenn man sucht wird das gleich geigt leidet :)
set hlsearch
" Es wird bereits gesscrollt wenn man 7 zeilen vom ende ist, nicht erst wenn man auf der letzten Zeile ist.
set scrolloff=7
" Schönes menue wird eingeplendent
set wildmenu
set foldcolumn=1

" Das undo über geschlossene Dateien möglich machen
set undofile
set undodir=$HOME/.vimundos/
